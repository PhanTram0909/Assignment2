<Window x:Class="PhanNgocBaoTramWPF.Views.CustomerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Customer Info"
        Height="320"
        Width="400"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Full Name:" Grid.Row="0" Margin="0,5,0,0"/>
        <TextBox x:Name="txtFullName" Grid.Row="0" Margin="90,5,0,0" Width="250" HorizontalAlignment="Left"/>

        <TextBlock Text="Email:" Grid.Row="1" Margin="0,10,0,0"/>
        <TextBox x:Name="txtEmail" Grid.Row="1" Margin="90,10,0,0" Width="250" HorizontalAlignment="Left"/>

        <TextBlock Text="Phone:" Grid.Row="2" Margin="0,10,0,0"/>
        <TextBox x:Name="txtPhone" Grid.Row="2" Margin="90,10,0,0" Width="250" HorizontalAlignment="Left"/>

        <TextBlock Text="Birthday:" Grid.Row="3" Margin="0,10,0,0"/>
        <DatePicker x:Name="dpBirthday" Grid.Row="3" Margin="90,10,0,0" Width="150" HorizontalAlignment="Left"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Save" Width="80" Margin="4" Click="BtnSave_Click"/>
            <Button Content="Cancel" Width="80" Margin="4" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
